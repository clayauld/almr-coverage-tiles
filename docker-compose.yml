version: '3.8'

services:
  web:
    image: caddy:alpine
    container_name: almr-coverage-viewer
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/caddy:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    restart: unless-stopped
